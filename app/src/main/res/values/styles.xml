<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="tabHeader">
        <item name="android:textSize">@dimen/tab_size</item>
    </style>

    <style name="AppThemeprice" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="StockTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabTextAppearance">@style/StockTextAppearance</item>
        <item name="tabIndicatorHeight">1dp</item>
        <item name="tabMode">fixed</item>
    </style>

    <style name="StockTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">30sp</item>
    </style>

    <style name="spinnerSize" parent="Widget.AppCompat.Spinner">
        <item name="android:textSize">30sp</item>
    </style>

   </resources>
